contoh komunikasi middleware dengan front end
middleware menggunakan C++ akan dibuild berupa dll
kemudian file dll ditambahkan ke dalam project front end
pada project front end tambahkan bridge untuk komunikasi dengan dll tersebut

Catatan : keduanya harus berjalan pada target CPU yang sama ( pada contoh ini x86 )
